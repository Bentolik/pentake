{
  "name": "pentake-unified",
  "version": "1.0.0",
  "description": "Unified and secured Pentake application",
  "scripts": {
    "install-all": "npm install --prefix server",
    "start": "npm start --prefix server",
    "test": "npm test --prefix server"
  }
}
